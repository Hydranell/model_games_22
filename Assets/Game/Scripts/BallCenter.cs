using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BallCenter : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //vektor von kugelmeitte zu mitte der map (center.obj), dann if collision von cube-kugel then cube um kugel drehen mittelpounkt der drehung = kugelmittelpunkt,
    //zielrichtung negative vektor z x vom kugelmittelpunkt zur map mittelpunkt mit kugelmittelpunkt-cubemittelpunkt als länge
}
